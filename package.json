{
  "name": "primer",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build -w @primer/react",
    "setup": "./script/setup",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "8.0.2"
  }
}
