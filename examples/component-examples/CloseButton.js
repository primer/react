import React from 'react'
import {Block, CloseButton} from '../../src'

const example = {
  name: 'CloseButton',
  element: (
    <Block p={4}>
      <CloseButton />
    </Block>
  )
}

export default example
