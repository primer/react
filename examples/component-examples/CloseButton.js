import React from 'react'
import {Box, CloseButton} from '../../src'

const example = {
  name: 'CloseButton',
  element: (
    <div>
      <Box className="clearfix">
        <CloseButton />
      </Box>
    </div>
  )
}

export default example
