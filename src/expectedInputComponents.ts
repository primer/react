import {Autocomplete, Checkbox, Radio, Select, Textarea, TextInput, TextInputWithTokens} from './'

export const expectedInputComponents = [Autocomplete, Checkbox, Radio, Select, TextInput, TextInputWithTokens, Textarea]
