export type ChildWidth = {text: string; width: number}
export type ChildWidthArray = ChildWidth[]
export type ResponsiveProps = {
  items: Array<React.ReactElement>
  actions: Array<React.ReactElement>
}
