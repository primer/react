import styled from 'styled-components'
import {
  space,
  width,
  flex,
  color
} from 'styled-system'

const ExampleBox = styled.div`
  >div {
    display: block;
    margin-bottom: 16px;
  }
  ${space}
  ${width}
  ${flex}
  ${color}
`

export default ExampleBox
