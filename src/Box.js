import styled from 'styled-components'
import {
  space,
  width,
  maxWidth,
  flex,
  color,
  textAlign
} from 'styled-system'

const Box = styled.div`
  ${space}
  ${width}
  ${maxWidth}
  ${flex}
  ${color}
  ${textAlign}
`

export default Box
