---
'@primer/react': minor
---

Export `getResponsiveAttributes` from `@primer/react/experimental`. This is the helper referenced by the `useResponsiveValue` deprecation notice and ADR-018 for bridging responsive prop values to CSS via `data-*` attributes, previously only available internally.
