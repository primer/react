name: Temporary
on:
  pull_request:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  get_cached_value:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: commit
        run: |
          git commit -am --allow-empty "[skip actions] Empty commit"
          git push origin HEAD
