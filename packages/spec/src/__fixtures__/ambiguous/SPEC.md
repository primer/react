# Ambiguous spec

Ambiguous has both supported component spec layouts.

## Features

### Default

The default feature exists in the single-file spec.

#### Behavior

The feature MUST be declared once.
