import {describe, expect, test} from 'vitest'
import {render} from '@testing-library/react'
import {OcticonSymbols} from '../OcticonSymbols'

describe('OcticonSymbols', () => {
  test('should work', () => {
    render(<OcticonSymbols />)
  })
})
