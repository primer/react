#!/bin/bash

set -e

# Clean up
npm run clean

# Bundle both ESM and CommonJS
npx rollup -c

# Note: Skipping TypeScript declaration generation for now
# due to import issues with @primer/react types
# TODO: Fix type imports and re-enable:
# npx tsc --project tsconfig.build.json
# npx copyfiles -u 1 "./lib/**/*.d.ts" ./lib-esm
