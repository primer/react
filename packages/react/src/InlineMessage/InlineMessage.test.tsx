import React from 'react'
import {InlineMessage} from '../InlineMessage'

describe('InlineMessage', () => {
  it('should use the info variant by default', () => {
    //
  })

  it('should support specifying the variant of the message', () => {
    //
  })

  it('should announce children assertively when variant="critical"', () => {
    //
  })

  it('should announce children politely when variants are not `critical`', () => {
    //
  })

  it('should forward props onto the outermost element', () => {
    //
  })
})
