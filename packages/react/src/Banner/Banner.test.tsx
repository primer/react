import {render, screen} from '@testing-library/react'
import {Banner} from '../Banner'

describe('Banner', () => {
  test('title', () => {
    //
  })

  test('description', () => {
    //
  })

  test('title + description', () => {
    //
  })

  test('actions', () => {
    //
  })

  test('dismissible', () => {
    //
  })

  test('extra props', () => {
    //
  })

  test('critical cannot be dismissible', () => {
    //
  })

  test('if only title is set', () => {
    //
  })
})
